// Part 1
$(function () {
    $('[data-toggle="popover"]').popover()
  });
// Step 1: set up a click event listener for the theme-button class.
$(".theme-button").click(function() {

});

// Step 2: create a variable named "theme" to store the name of the theme that was just clicked.
var theme = $("#theme-stylesheet")

// Step 3: create another variable called "$styleLink" to store the link with the id 'theme-stylesheet'
var $styleLink = $("#styleLink")

// Step 4: using if/else conditionals, update the stylesheet according to the button that was clicked (the button/theme name). For example, if the 'darkly' theme button was clicked, update the link element's href attribute to point at the .css file for the darkly theme.
if
else

// Part 2

// Step 1: initialize Bootstrap popovers.
// HINT: it's in the Bootstrap docs
